
## 2.3.0 Builder UI 2.4 FIX10
- Unified Section 2 header typography and foreground color with the rest of the tactical UI.
- Localized Advanced Mode application settings panel in PL and EN.
- Removed hard-coded Polish settings text from the visual layer so language switching updates the section correctly.
- Added an explicit SectionHeaderButton style to prevent native WPF blue/default button rendering from changing font and text color.


## 2.3.0 Builder UI 2.4 FIX7

- Advanced Mode uses a compact two-column layout sized to the visible application viewport.
- Main application canvas binds to `ScrollViewer.ViewportWidth` so Full HD fits the screen width without forced horizontal expansion.
- Advanced Server/ATAK controls are now the live controls used by the existing 2.3 engine; duplicate hidden controls were removed.
- Added a dedicated `BUILDER ADVANCED COMPACT FHD CONTRACT` test.

### 2.3.0 Builder UI 2.4 FIX6
- Usunięto przycinanie kart w sekcji zawartości paczki przez przejście na auto-sizing.
- Dodano rozwijanie sekcji **Zawartość paczki** i **Dodatkowe opcje**.
- Zastąpiono lewy dolny artwork dokładnym dostarczonym obrazem z operatorem terenowym.
- Dodano odporną inicjalizację `source/` i `out/`, gdy pola ścieżek są puste.
- Zachowano wspólny pionowy i poziomy scroll całej aplikacji.


## 2.3.0 — UI 2.4 FHD navigation / scrolling update

- Functional left navigation with full-page scroll positioning.
- Full application vertical + horizontal scrolling instead of nested advanced-mode scrolling.
- Editable package and QR validity in hours, with quick preset buttons.
- New left-bottom tactical Field TAK Hub artwork.
- Dedicated Advanced Settings section with PL/EN quick actions.
- Updated UI 2.4 contract tests.

# Changelog

## 2.3.0 — Simple Mode optional steps

### Android
- added explicit skip controls for optional Meshtastic, enrollment, Data Package and radio-configuration steps in Simple Mode;
- skipped optional actions are recorded as `SKIPPED` and do not appear as deployment failures;
- required ATAK, plugin, package-integrity and publisher-trust steps remain non-skippable;
- skipped Meshtastic does not block the Simple Mode readiness flow.


## 2.3.0 — Meshtastic role mapping fix

- `FIELD NODE` in Builder is now mapped to native Meshtastic `TAK` role in generated project/package profiles.
- Existing projects using legacy `field_node` remain readable and are migrated to `tak` on load.
- Gateway behavior remains unchanged.

## 2.3.0 — Test release

### Android

- Simplified Simple Mode for field users.
- Added structured Advanced Mode with clear OpenTAKServer sections for **User Enrollment** and **Data Packages**.
- Retained `.ftak` import as a first-class action.
- Added one-tap Meshtastic configuration handoff when the package contains a prepared channel configuration.
- Added Smart Diagnosis and Recovery workflow.
- Improved Meshtastic runtime/link diagnostics.
- Added normal back navigation between internal screens.
- Added double-back protection on the home screen before moving the app to the background.
- Added updated Field TAK Hub application icon.
- Added bilingual author-support panel with BuyCoffee QR and link.
- Maintained Polish / English resource parity.

### Meshtastic

- HYBRID deployment remains hardware-neutral.
- `channel.url` and optional `profile.json` remain supported.
- Gateway metadata is kept separate from radio I/O and secrets.
- BLE/USB and radio communication remain owned by the Meshtastic/ATAK layer.

### Windows Builder

- Project folder automation retained.
- `.ftak` schema v2 retained.
- Enrollment, Data Package and Meshtastic packaging retained.
- Package validity and QR validity settings retained.

### CI / validation

- Source Gate and static contract suite included in the repository.
- Android, Windows Builder and release GitHub Actions workflows retained.
- Release artifacts are generated by CI rather than committed to Git.
