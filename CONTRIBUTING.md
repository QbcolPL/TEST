# Contributing

1. Create a feature branch.
2. Keep `.ftak` schema changes backward-compatible when possible.
3. Add/update tests when changing package signing or verification.
4. Never add real TAK credentials/certificates to fixtures.
5. Run Windows and Android builds before opening a PR.
