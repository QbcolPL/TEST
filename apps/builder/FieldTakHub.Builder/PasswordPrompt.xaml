<Window x:Class="FieldTakHub.Builder.PasswordPrompt" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource PasswordTitle}" Width="420" Height="190" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
  <Grid Margin="18"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
    <TextBlock Text="{DynamicResource PasswordPrompt}" TextWrapping="Wrap"/>
    <PasswordBox x:Name="PasswordBox" Grid.Row="1" Margin="0,12,0,12" MinHeight="34" AutomationProperties.Name="{DynamicResource PasswordPrompt}"/>
    <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
      <Button Content="{DynamicResource Cancel}" Click="Cancel_Click" IsCancel="True"/>
      <Button Content="{DynamicResource Ok}" Click="Ok_Click" IsDefault="True"/>
    </StackPanel>
  </Grid>
</Window>
